import { describe, it } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import TimerControl from '../components/TimerControl';

describe('Tests for TimerControl component', () => {
  
  it('renders TimerControl', () => {
    render(<TimerControl />);

  })

})